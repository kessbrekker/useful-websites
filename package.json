{
    "name": "useful-websites",
    "version": "1.0.0",
    "description": "A modern website directory platform",
    "scripts": {
        "dev": "wrangler pages dev public --live-reload --d1 DB",
        "deploy": "wrangler pages deploy public",
        "build": "echo 'Build step complete'"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    },
    "dependencies": {
        "jose": "^6.1.3"
    }
}